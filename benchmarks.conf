acasxu
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

cifar2020
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

eran
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

mnistfc
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

nn4sys
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

oval21
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

test
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>

__default__
	___ # Number of Abstractions <int>
	___ # FGSM Balance Factor <float>
	___ # Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	___ # Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___ # Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
	    # Input Node (optional -- just leave empty) <string>
	    # Output Node (optional -- just leave empty) <string>
	    # Path to Labels.pb (optional -- just leave empty) <string>
	    # Path to Class_Averages.pb (optional -- just leave empty) <string>
