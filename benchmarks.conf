acasxu
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

cifar10_resnet
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	0.00390625	# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

cifar2020
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	0.00390625	# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

eran
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

marabou-cifar10
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	0.00390625	# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

mnistfc
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	0.00390625	# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

nn4sys
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

oval21
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

test
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

verivital
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>

__default__
	10		# Number of Abstractions <int>
	0.9		# FGSM Balance Factor <float>
	gradient_based	# Modified FGSM Dimension Selection <string in ["intellifeature", "gradient_based"]>
	largest_first	# Refinement Dimension Selection <string in ["intellifeature", "gradient_based", "random", "largest_first"]>
	___		# Granularity <float> or <float[]> [End a float value with "x" to multiply by the radius -- <float[]> only]
			# Input Node (optional -- just leave empty) <string>
			# Output Node (optional -- just leave empty) <string>
			# Path to Labels.pb (optional -- just leave empty) <string>
			# Path to Class_Averages.pb (only required if using intellifeature -- just leave empty) <string>
